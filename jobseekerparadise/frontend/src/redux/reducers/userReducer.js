// actions import

const initialState = {
    user: {
        username: null,
        password: null
    },
    token: null
}

export const userReducer = (state = initialState, action) => {
    return state
}